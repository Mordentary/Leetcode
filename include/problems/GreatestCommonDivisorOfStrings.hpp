#pragma once
#include<string>
#include<vector>

namespace problems::easy
{
	//todo
	static inline std::string findStringPattern(std::string& str)
	{
		std::string currentFoundPattern = str[0];

		while (currentFoundPattern.)
		{
		}
	}

	std::string gcdOfStrings(std::string str1, std::string str2)
	{
		std::string pattern = findStringPattern(str2);
	}
}